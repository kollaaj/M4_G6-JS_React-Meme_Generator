import React from 'react';
import ReactDOM from 'react-dom';
import './styles.css';
import App from './App';

// rendering the app component (what, where)
ReactDOM.render(<App />, document.querySelector('#root'));
